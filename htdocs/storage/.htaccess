# Dieses Verzeichnis enthält Laufzeitdaten (Rate-Limit, Protokoll, Secret) und darf nicht ausgeliefert werden.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
