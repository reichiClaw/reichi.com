# Dieses Verzeichnis enthält nur PHP-Code und darf nicht direkt aufgerufen werden.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
